"use strict";
class Pagina {
    constructor(currentpage, nextpage, previouspage, totalpage) {
        this.currentpage = currentpage;
        this.nextpage = nextpage;
        this.previouspage = previouspage;
        this.totalpage = totalpage;
    }
}
//# sourceMappingURL=Pagina.js.map